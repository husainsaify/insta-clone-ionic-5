import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cloneInsta.com',
  appName: 'instagramClone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
